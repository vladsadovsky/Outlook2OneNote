// Ribbon button handler — opens the task pane
// TODO: implement — AC-05.2

Office.onReady(() => {
  Office.actions.associate('action', (_event: Office.AddinCommands.Event) => {
    // TODO: open task pane
  })
})
